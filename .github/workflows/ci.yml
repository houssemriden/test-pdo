name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  phase-1:
    name: Phase 1
    runs-on: ubuntu-latest
    steps:
      - name: Hello world 1
        run: echo "Hello world 1"

  phase-2:
    name: Phase 2
    runs-on: ubuntu-latest
    needs: phase-1
    steps:
      - name: Hello world 2
        run: echo "Hello world 2"
